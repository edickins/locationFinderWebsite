import { describe, it, expect, test } from 'vitest';
import { render, screen } from '@testing-library/react';
import

describe('FilterPanel', () => {
  test('should render all nav buttons', () => {

  })

});
